# Changelog

## 2.1.1

- азимут між `North +` і бісектрисою тепер показується в повному діапазоні `0–359°`;
- відлік ведеться за годинниковою стрілкою: North `0°`, East `90°`, South `180°`, West `270°`;
- синя дуга тепер може проходити повне коло, а не лише найкоротший сектор `0–180°`;
- межі вікна враховують довгі дуги азимуту до `359°`.

## 2.1.0

- версію 2.0.0 зі splash-вікном відкинуто; код побудовано на стабільній базі 1.9.1;
- у контекстне меню додано перемикач `North +` із галочкою активного стану;
- додано стрілку магнітної півночі, спрямовану вертикально вгору, та позначення `N`;
- при ввімкненій бісектрисі малюється синя дуга від North до бісектриси;
- біля дуги показується блок із величиною кута;
- стрілка адаптується до режиму `Інверсія`;
- стан `North +` зберігається у файлі налаштувань версії 6.

## 1.9.1

- при одночасно закритих синьому та червоному замках повністю заблоковано зміну кута й обертання системи;
- колесико над блоком градусів та над замками не змінює геометрію, доки обидва замки закриті;
- перетягування будь-якої червоної точки при двох закритих замках змінює лише спільну довжину променів без зміни їх напрямків;
- подвійний клік по червоній точці переносить центр гіпотенузи на зелений плюс, залишаючи синю вершину нерухомою;
- під час такого вирівнювання зберігаються поточний кут і однакова довжина червоних променів.

## 1.8.1

- Подвійний клік по будь-якій червоній точці переносить основну систему так, щоб центр гіпотенузи збігся із зеленим плюсом.
- Біля обох червоних точок додано компактні замки із зовнішнього боку основного кута. Одночасно активним може бути один червоний замок.
- При закритих синьому та червоному замках глобальне обертання блокується; протилежну червону точку можна переміщувати для зміни створу без повороту зафіксованого базисного променя.
- При закритому червоному та відкритому синьому замку синя вершина і друга червона точка обертаються навколо зафіксованої червоної точки зі збереженням геометрії кута.
- Стан червоного замка зберігається у налаштуваннях.

## 1.7.0

- подвійний клік по зеленому плюсу переносить його в центр червоної гіпотенузи;
- радіус зелених допоміжних дуг прив’язано до 50% довжини лінії синя вершина → плюс;
- при виході плюса за гіпотенузу додаються червоні пунктирні продовження бокових променів;
- якщо плюс не доходить до гіпотенузи, його промінь продовжується зеленим штрихпунктиром до перетину;
- при виході плюса за межі основного кута дуги стають червоними та з’являється блок `Δ` з кутом виходу;
- пункт меню `+ градуси` перейменовано на `Градуси +`.

## 1.6.0

- додано режим контекстного меню `Дистанція +` із галочкою активного стану;
- поточна лінія від синьої вершини до зеленого плюса автоматично калібрується як `1000м`;
- додано блоки дистанцій для базової лінії, двох червоних променів, гіпотенузи та ліній `Фронт +`;
- подвійний клік по блоку відкриває вбудоване числове редагування;
- підтримано `Enter`, `Esc`, `Backspace`, крапку та кому;
- зміна базової дистанції перекалібровує всі значення;
- зміна дистанції червоного променя синхронно перебудовує обидва промені;
- зміна гіпотенузи перебудовує радіус червоної системи за поточним кутом;
- зміна дистанції `Фронт +` перебудовує червону систему вздовж відповідного променя;
- стан режиму та масштаб метрів зберігаються у файлі налаштувань версії 4;
- додано окремий модуль `src/distance.rs`.

## 1.5.0

- додано перемикачі `Бісектриса`, `Плюс`, `Градуси +`, `Інверсія`, `Гіпотенуза` та `Фронт +`;
- активні пункти меню позначаються галочкою;
- зелений маркер замінено тонким плюсом;
- додано обертання заблокованої червоної системи колесиком над замком;
- додано інверсію чорних ліній у білі;
- додано червону пунктирну гіпотенузу та зелені пунктирні лінії `Фронт +`.

## 1.4.2

- додано фірмовий ярлик ProtractorPlus;
- один ярлик використовується у вікні, на панелі задач і всередині Windows `.exe`.

## 1.4.1

- лінію до зеленого плюса змінено на чорний штрихпунктир `_ . _ . _ .`;
- блоки підкутів розміщено на їхніх бісектрисах.

## 1.4.0

- додано допоміжний зелений елемент, дві дуги та значення підкутів.

## 1.3.1

- текст Comfortaa рендериться у 4-кратній внутрішній роздільній здатності.

## 1.3.0

- вбудовано Comfortaa;
- замок зменшено вдвічі;
- додано коригування кута колесиком.
